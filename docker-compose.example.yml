services:
  db:
    image: mcr.microsoft.com/azure-sql-edge:latest
    container_name: warehouse-db
    environment:
      ACCEPT_EULA: "1"
      MSSQL_SA_PASSWORD: "Enter_A_Strong_Pass123"
    ports:
      - "1433:1433"
    volumes:
      - mssql-data:/var/opt/mssql

volumes:
  mssql-data:
